version: '3.8'

services:
  web:
    build: .
    command: gunicorn ludex_project.wsgi:application --bind 0.0.0.0:8000
    ports:
      - "80:8000"
    env_file:
      - .env
    restart: unless-stopped
